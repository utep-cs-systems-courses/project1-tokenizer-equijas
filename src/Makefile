#target will be desired executable name

#define variable $(OBJS) to list of object files
OBJS= uimain.o

# make has a "default rule" specifying how to build a an "object" file (.o) from a C source file (.c)
#            this rule includes $(CFLAGS) as one of cc's parameters 
CFLAGS=-g -O3 # -g for debug, -O3 for optimization

# first target is built by default.  Usually named "all"
all: prompt

# PROMPT (target) depends on all object files (prerequisites)
prompt: $(OBJS) 
	cc -o prompt $(CFLAGS) $(OBJS)

# all object files built from c files that include its header file
$(OBJS): uimain.h

# deletes files generated by compilation
clean:
	rm -f *.o prompt

Run:prompt
	./prompt
